import 'package:flutter/material.dart';

class SelectedBooksPage extends StatelessWidget {
  const SelectedBooksPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('aaa'),
        ),
      );
}
